<tr>
  <td class="user-edit"><%= link_to user.name, user_path(user) %></td>
  <td class="user-edit"><%= user.email %></td>
  <% unless user.admin? %>
    <td class="user-edit">
      <%= link_to t(:edit), edit_admin_user_path(user),
        class: "btn btn-primary" %>
    </td>
  <% end %>
</tr>
